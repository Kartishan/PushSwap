#include "struct.h"
#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>

// void	free(t_stack **a)
// {
// 	t_stack	*s;

// 	s = (*a)->next;
// 	while (a != NULL)
// 	{
// 		free((*a));
// 		(*a) = s;
// 		s = s->next;
// 	}
// }
